services:
  stella:
    build:
      context: ..
      dockerfile: agent-stella/Dockerfile
    container_name: agent-stella
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./stella-data:/app/data
    shm_size: '2gb'
    deploy:
      resources:
        limits:
          memory: 4G
